{
  "name": "grailwatch",
  "version": "1.0.0",
  "description": "GrailWatch — on-chain proof-of-reserves & bridge transparency for zkLTC on LitVM LiteForge",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network liteforge"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.26.0",
    "dotenv": "^16.4.5"
  }
}
